#include <iostream>
#include <vector>

using namespace std;

int main() {

    vector<int> vector;

    vector.push_back(5);

    // vector.swap(vector2); faz a troca de elementos entre dois vectors
    // vector.insert(vector.begin + 1, num); insere um elemento em uma posicao de um vector. E possivel usar o endereco

    return 0;

}
